# Core Discord
discord.py==2.3.2
PyNaCl==1.5.0

# Async + Networking
aiohttp==3.9.3
asyncio==3.4.3

# Database
asyncpg==0.29.0

# Cache (Redis)
redis==5.0.1

# Parsing / Scraping
beautifulsoup4==4.12.3
lxml==5.1.0

# Environment
python-dotenv==1.0.1

# Security / Crypto
cryptography==42.0.5

# Performance (Linux only)
uvloop==0.19.0 ; sys_platform != "win32"

# ==================================================
# 🔥 NEW: Twitch + EventSub + Web + Infra
# ==================================================

# Web framework (EventSub + webhook server)
aiohttp[speedups]==3.9.3

# Optional but strongly recommended for JSON + validation
pydantic==2.6.4

# Logging + observability (structured logs zaten var ama iyi olur)
orjson==3.9.15

# Retry / resilience (EventSub + API calls için)
tenacity==8.2.3

# HTTP utilities (optional but useful)
httpx==0.27.0

# Time handling (Twitch timestamps vs)
python-dateutil==2.9.0.post0
