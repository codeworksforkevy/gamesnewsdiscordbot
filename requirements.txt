# Core Discord
discord.py==2.4.0
audioop-lts==1.2.1.post0   # audioop was removed in Python 3.13; this restores it
PyNaCl==1.5.0

# Async + Networking
aiohttp==3.10.5          # 3.9.x has no Python 3.13 wheels

# Database
asyncpg==0.30.0          # 0.29.x has no Python 3.13 wheels

# Cache (Redis)
redis[asyncio]==5.0.1

# Parsing / Scraping
beautifulsoup4==4.12.3
lxml==5.3.0              # 5.1.x has no Python 3.13 wheels

# Environment
python-dotenv==1.0.1

# Security / Crypto
cryptography==42.0.5

# Performance (Linux only)
uvloop==0.21.0 ; sys_platform != "win32"  # 0.19.x does not support Python 3.13

# ==================================================
# Twitch + EventSub + Infra
# ==================================================

# Web server
fastapi==0.115.5          # 0.110.x requires pydantic-core which has no 3.13 wheels
uvicorn[standard]==0.27.1

# Validation — pydantic 2.10+ ships pydantic-core with Python 3.13 wheels
pydantic==2.10.6

# JSON
orjson==3.10.7           # 3.9.x has no Python 3.13 wheels

# Retry
tenacity==8.2.3

# HTTP
httpx==0.27.0

# Date utils
python-dateutil==2.9.0.post0
